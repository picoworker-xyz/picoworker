# Copy to .env.local and fill in to switch the app from the local mock store
# to a real Supabase backend. Find these in your Supabase project:
# Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
