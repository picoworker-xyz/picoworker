// Display-name fallback for the provider review queue when an earner's profile
// name isn't loaded. Real names come from the profiles table; this is just a
// label for known demo ids and otherwise falls back to "Earner".
export const DEMO_EARNER_NAMES: Record<string, string> = {}
